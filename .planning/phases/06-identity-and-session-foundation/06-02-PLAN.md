---
phase: 06-identity-and-session-foundation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - BitNest/Program.cs
  - BitNest/Controllers/AuthController.cs
  - BitNest/Services/AuthService.cs
  - BitNest/Services/JwtTokenService.cs
  - BitNest/Services/PasswordHasher.cs
  - BitNest/Data/AppDbContext.cs
  - BitNest/appsettings.json
  - BitNest/DTOs/Auth/*.cs
  - BitNest.Tests/Auth/AuthEndpointTests.cs
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "Valid credentials issue access + refresh session credentials"
    - "Refresh endpoint rotates refresh session and invalidates previous token"
    - "Logout revokes refresh session and rejects later reuse"
    - "Protected auth identity endpoint requires valid access token"
  artifacts:
    - path: "BitNest/Controllers/AuthController.cs"
      provides: "`/auth/*` endpoint surface"
    - path: "BitNest/Services/AuthService.cs"
      provides: "Credential validation and refresh rotation logic"
    - path: "BitNest/Program.cs"
      provides: "Authentication middleware pipeline"
  key_links:
    - from: "BitNest/Controllers/AuthController.cs"
      to: "BitNest/Services/AuthService.cs"
      via: "controller delegates auth operations"
      pattern: "authService\.(Signup|Login|Refresh|Logout|GetMe)"
    - from: "BitNest/Program.cs"
      to: "BitNest/Services/JwtTokenService.cs"
      via: "JWT bearer validation configuration"
      pattern: "AddAuthentication\("
---

<objective>
Implement backend authentication APIs and middleware to provide login, refresh rotation, logout revocation, and identity lookup.

Purpose: Deliver all server-side session behavior required by AUTH-02/03/04 and locked endpoint contracts.
Output: `/auth/*` endpoints, JWT+refresh lifecycle services, auth DTOs, and endpoint tests.
</objective>

<execution_context>
@/home/danylo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/danylo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-identity-and-session-foundation/06-CONTEXT.md
@.planning/phases/06-identity-and-session-foundation/06-RESEARCH.md
@.planning/phases/06-identity-and-session-foundation/06-01-SUMMARY.md
@BitNest/Program.cs
@BitNest/Controllers/StorageController.cs

<interfaces>
From `BitNest/Program.cs`:
```csharp
builder.Services.AddControllers();
builder.Services.AddDbContext<AppDbContext>(...);
app.UseAuthorization();
app.MapControllers();
```

From locked decisions in context:
```text
/auth/signup
/auth/login
/auth/refresh
/auth/logout
/auth/me
Error shape: { code, message }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add auth endpoint tests for login/refresh/logout/me contracts</name>
  <files>BitNest.Tests/Auth/AuthEndpointTests.cs, BitNest.Tests/BitNest.Tests.csproj</files>
  <read_first>BitNest.Tests/Auth/AuthModelValidationTests.cs, BitNest/Controllers/StorageController.cs, .planning/phases/06-identity-and-session-foundation/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `POST /auth/login` with invalid credentials returns `401` and JSON containing `code` and `message`.
    - Test 2: `POST /auth/refresh` rotates token so old refresh token is rejected.
    - Test 3: `POST /auth/logout` revokes refresh token and subsequent refresh attempt fails.
    - Test 4: `GET /auth/me` returns `401` without bearer and `200` with valid bearer.
  </behavior>
  <action>Create integration-oriented auth endpoint tests in `AuthEndpointTests.cs` that assert status codes and response bodies for `/auth/login`, `/auth/refresh`, `/auth/logout`, and `/auth/me`. Ensure assertions explicitly check that auth error responses include both `code` and `message` keys and that refresh rotation invalidates the previous token/session.</action>
  <acceptance_criteria>`BitNest.Tests/Auth/AuthEndpointTests.cs` contains test methods named `Login_returns_401_with_stable_error_shape_for_invalid_credentials`, `Refresh_rotates_and_invalidates_previous_token`, `Logout_revokes_refresh_session`, and `Me_requires_valid_access_token`.</acceptance_criteria>
  <verify>
    <automated>dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~AuthEndpointTests"</automated>
  </verify>
  <done>Executable tests define the required auth endpoint behavior before production endpoint implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auth services, DTOs, and `/auth/*` controller endpoints</name>
  <files>BitNest/Controllers/AuthController.cs, BitNest/Services/AuthService.cs, BitNest/Services/JwtTokenService.cs, BitNest/DTOs/Auth/*.cs, BitNest/Data/AppDbContext.cs, BitNest/Services/PasswordHasher.cs</files>
  <read_first>BitNest/Controllers/StorageController.cs, BitNest/Services/StorageService.cs, BitNest/Models/User.cs, BitNest/Models/RefreshSession.cs, .planning/phases/06-identity-and-session-foundation/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Signup/login issue access token and refresh token for valid credential flows.
    - Test 2: Refresh path rotates refresh token and revokes old session in a single transaction.
    - Test 3: Logout revokes active refresh session and clears cookie/token output.
    - Test 4: Duplicate usernames are rejected with deterministic error code/message.
  </behavior>
  <action>Create `AuthController` with route prefix `[Route("auth")]` and endpoints `POST signup`, `POST login`, `POST refresh`, `POST logout`, and `[Authorize] GET me`. Implement `AuthService` to validate username format (`^[a-z0-9._-]+$` after lowercase normalization), enforce password minimum length 8, hash passwords through `PasswordHasher`, create/revoke/rotate refresh sessions, and produce standard auth error DTO `{ code, message }`. Implement `JwtTokenService` to create 15-minute access JWTs with user id + normalized username claims and to generate opaque refresh secrets; persist only refresh token hashes in `RefreshSessions`.</action>
  <acceptance_criteria>`BitNest/Controllers/AuthController.cs` contains methods for `signup`, `login`, `refresh`, `logout`, and `me`; `BitNest/Services/AuthService.cs` contains methods `Signup`, `Login`, `Refresh`, and `Logout`; auth error DTO includes properties `Code` and `Message`; refresh persistence writes `TokenHash` and never stores plaintext token.</acceptance_criteria>
  <verify>
    <automated>dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~AuthEndpointTests"</automated>
  </verify>
  <done>All required backend auth endpoints exist and satisfy locked API contracts for session lifecycle behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Wire JWT bearer authentication and auth config into Program startup</name>
  <files>BitNest/Program.cs, BitNest/appsettings.json, BitNest/BitNest.csproj</files>
  <read_first>BitNest/Program.cs, BitNest/appsettings.json, BitNest/BitNest.csproj</read_first>
  <action>Add JWT authentication services and middleware in `Program.cs`: call `AddAuthentication().AddJwtBearer(...)`, keep existing CORS behavior, and ensure `app.UseAuthentication()` executes before `app.UseAuthorization()`. Add explicit `Auth` configuration section in `appsettings.json` with issuer, audience, access-token minutes (`15`), and signing key placeholder value for local development. Add any required package references for JWT bearer auth in `BitNest.csproj` if not already present.</action>
  <acceptance_criteria>`BitNest/Program.cs` contains `AddAuthentication(`, `AddJwtBearer(`, and `app.UseAuthentication();` before `app.UseAuthorization();`; `BitNest/appsettings.json` contains `"Auth"` object with keys `Issuer`, `Audience`, `AccessTokenMinutes`, and `SigningKey`.</acceptance_criteria>
  <verify>
    <automated>dotnet build BitNest.sln</automated>
  </verify>
  <done>Auth middleware is active and endpoint authorization can enforce bearer token checks.</done>
</task>

</tasks>

<verification>
Run endpoint test slice and compile everything:
- `dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~AuthEndpointTests"`
- `dotnet build BitNest.sln`
</verification>

<success_criteria>
- `/auth/login`, `/auth/refresh`, `/auth/logout`, and `/auth/me` are implemented with stable error shape.
- Refresh flow rotates tokens and old refresh tokens are invalidated.
- JWT middleware is wired and protects authenticated endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/06-identity-and-session-foundation/06-02-SUMMARY.md`
</output>
