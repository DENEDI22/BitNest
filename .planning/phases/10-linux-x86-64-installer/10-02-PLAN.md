---
phase: 10-linux-x86-64-installer
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - installers/linux-x86_64/install.py
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-04
  - INST-07

must_haves:
  truths:
    - "User sees a main menu with Install/Update/Uninstall options on app launch"
    - "Main menu pre-highlights Update if state file exists, Install if not (D-02)"
    - "Step 1 shows prerequisite check results with pass/fail icons"
    - "Step 2 collects install dir, ports, admin credentials with inline validation on Next"
    - "Step indicator shows current step highlighted across all wizard screens"
    - "Back navigation works from Step 2 to Step 1 and from Step 1 to main menu"
  artifacts:
    - path: "installers/linux-x86_64/install.py"
      provides: "Textual TUI screens: MainMenuScreen, Step1PrerequisitesScreen, Step2ConfigurationScreen, StepIndicator"
      contains: "class MainMenuScreen"
    - path: "installers/linux-x86_64/install.py"
      provides: "Textual App subclass with screen navigation"
      contains: "class InstallerApp"
  key_links:
    - from: "MainMenuScreen"
      to: "Step1PrerequisitesScreen"
      via: "push_screen on Install selection"
      pattern: "push_screen.*Step1"
    - from: "Step1PrerequisitesScreen"
      to: "Step2ConfigurationScreen"
      via: "push_screen on Next Step press"
      pattern: "push_screen.*Step2"
    - from: "Step2ConfigurationScreen"
      to: "validate_port|validate_path|validate_username|validate_password"
      via: "calls core validation functions from Plan 01"
      pattern: "validate_port|validate_path"
---

<objective>
Build the Textual TUI: MainMenu, Step 1 Prerequisites, Step 2 Configuration screens, step indicator widget, and the InstallerApp shell.

Purpose: Implement the interactive portion of the installer — the screens the user sees and interacts with before installation begins. These screens wire into the core logic functions from Plan 01 (validation, preflight checks, state file reading). Steps 3/4 and Update/Uninstall screens come in Plan 03.

Output: install.py extended with Textual App, three Screen subclasses, step indicator, and Textual CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-linux-x86-64-installer/10-CONTEXT.md
@.planning/phases/10-linux-x86-64-installer/10-RESEARCH.md
@.planning/phases/10-linux-x86-64-installer/10-UI-SPEC.md
@.planning/phases/10-linux-x86-64-installer/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Textual CSS + StepIndicator + MainMenuScreen</name>
  <files>installers/linux-x86_64/install.py</files>
  <read_first>
    installers/linux-x86_64/install.py
    .planning/phases/10-linux-x86-64-installer/10-UI-SPEC.md
    .planning/phases/10-linux-x86-64-installer/10-CONTEXT.md
    .planning/phases/10-linux-x86-64-installer/10-RESEARCH.md
  </read_first>
  <action>
    Add the Textual TUI code to `install.py` BELOW the core logic functions (after the `# --- TUI --- #` marker from Plan 01). Add these components:

    **1. Textual auto-install bootstrap** (at very top of file, before all other imports):
    ```python
    import subprocess, sys
    try:
        import textual
    except ImportError:
        print("Installing UI dependency (textual)...")
        subprocess.run([sys.executable, "-m", "pip", "install", "--quiet", "textual>=0.89.0"], check=True)
        import textual
    ```

    **2. Textual CSS** as `InstallerApp.CSS` class variable. Colors from UI-SPEC:
    - App background: `#1a1a2e` (dominant 60%)
    - Panel backgrounds: `#16213e` (secondary 30%)
    - Accent: `#e94560` (highlight, buttons, focus rings)
    - Success: `#00b894`; Warning: `#fdcb6e`; Destructive: `#e17055`
    - Foreground: `#dfe6e9`; Dim: `#636e72`
    - Spacing: xs=1, sm=2, md=4, lg=6, xl=8 cell units

    Define CSS rules for:
    - `Screen { background: #1a1a2e; color: #dfe6e9; padding: 8; }`
    - `#title { text-style: bold; color: #e94560; margin-bottom: 2; }`
    - `.step-indicator { background: #16213e; padding: 1; margin-bottom: 2; }`
    - `.step-active { color: #e94560; text-style: bold; }`
    - `.step-inactive { color: #636e72; }`
    - `.success { color: #00b894; }` `.warning { color: #fdcb6e; }` `.destructive { color: #e17055; }`
    - `.dim { color: #636e72; }`
    - `Button { margin: 1 2; }` `Button.accent { background: #e94560; }` `Button.dim { background: #16213e; color: #636e72; }`
    - `Input:focus { border: tall #e94560; }`
    - `ListView > ListItem.-highlight { background: #e94560; }`

    **3. StepIndicator widget** — a `Static` subclass or `Horizontal` container with 4 Labels:
    ```python
    class StepIndicator(Static):
        def __init__(self, current_step: int = 1):
            super().__init__()
            self.current_step = current_step

        def render(self) -> str:
            steps = ["Prerequisites", "Configuration", "Installing", "Done"]
            parts = []
            for i, name in enumerate(steps, 1):
                if i == self.current_step:
                    parts.append(f"[bold #e94560][ Step {i}/4 {name} ][/]")
                else:
                    parts.append(f"[#636e72]  Step {i}/4 {name}  [/]")
            return "  ".join(parts)
    ```

    **4. MainMenuScreen** per UI-SPEC:
    - Title: `Label("BitNest Installer", id="title")` — bold, accent color
    - `ListView` with 3 `ListItem` entries: "Install BitNest" (id="install"), "Update BitNest" (id="update"), "Uninstall BitNest" (id="uninstall")
    - On mount: read state via `read_state()`. If state exists, set ListView index to 1 (Update highlighted per D-02). If no state, index 0 (Install highlighted).
    - `on_list_view_selected`: push Step1PrerequisitesScreen for "install", UpdateScreen for "update", UninstallScreen for "uninstall"
    - Key binding: `BINDINGS = [Binding("q", "quit", "Quit")]`
    - Footer with `Footer()` widget showing Q to quit

    **5. InstallerApp** shell:
    ```python
    class InstallerApp(App):
        CSS = """..."""  # (the CSS above)
        TITLE = "BitNest Installer"
        BINDINGS = [Binding("q", "quit", "Quit")]

        def on_mount(self) -> None:
            self.push_screen(MainMenuScreen())
    ```

    **6. Entry point** (at bottom of file):
    ```python
    if __name__ == "__main__":
        app = InstallerApp()
        app.run()
    ```
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/CsharpProjekte/BitNest && python -c "import importlib.util; spec = importlib.util.spec_from_file_location('install', 'installers/linux-x86_64/install.py'); mod = importlib.util.module_from_spec(spec); exec(compile(open('installers/linux-x86_64/install.py').read(), 'install.py', 'exec'))" 2>&1 | head -5 || python -c "import ast; ast.parse(open('installers/linux-x86_64/install.py').read()); print('SYNTAX OK')"</automated>
  </verify>
  <acceptance_criteria>
    - installers/linux-x86_64/install.py contains `class InstallerApp(App)`
    - installers/linux-x86_64/install.py contains `class MainMenuScreen(Screen)`
    - installers/linux-x86_64/install.py contains `class StepIndicator`
    - installers/linux-x86_64/install.py contains `#1a1a2e` (dominant background color)
    - installers/linux-x86_64/install.py contains `#e94560` (accent color)
    - installers/linux-x86_64/install.py contains `read_state()` called within MainMenuScreen
    - installers/linux-x86_64/install.py contains `push_screen(Step1PrerequisitesScreen`
    - installers/linux-x86_64/install.py contains `Binding("q"` or `("q",`
    - installers/linux-x86_64/install.py contains `if __name__ == "__main__":`
    - File parses without SyntaxError: `python -c "import ast; ast.parse(open('installers/linux-x86_64/install.py').read())"`
  </acceptance_criteria>
  <done>InstallerApp, MainMenuScreen, and StepIndicator implemented with Textual CSS matching UI-SPEC color contract. Main menu pre-highlights based on state file existence. App entry point functional.</done>
</task>

<task type="auto">
  <name>Task 2: Step1PrerequisitesScreen + Step2ConfigurationScreen</name>
  <files>installers/linux-x86_64/install.py</files>
  <read_first>
    installers/linux-x86_64/install.py
    .planning/phases/10-linux-x86-64-installer/10-UI-SPEC.md
    .planning/phases/10-linux-x86-64-installer/10-CONTEXT.md
  </read_first>
  <action>
    Add Step1PrerequisitesScreen and Step2ConfigurationScreen to `install.py`.

    **Step1PrerequisitesScreen** per UI-SPEC and D-05/D-06/D-07:
    - `compose()` yields: `StepIndicator(current_step=1)`, then a `Vertical` container with status rows, then `Horizontal` with Back and Next Step buttons
    - On mount, run prerequisite checks in a `@work(exclusive=True)` worker to avoid blocking the event loop:
      1. Docker Engine: call `check_docker()` — if installed show `[#00b894]✔[/]  Docker Engine          installed`; if missing show `[#fdcb6e]⏳[/]  Docker Engine          Docker will be installed automatically` per D-06
      2. Docker Compose V2: if docker installed, call `check_compose_version()` — show version or ✗
      3. Port 5000: call `is_port_free(5000)` — show ✔ or `[#e17055]✗[/]  Port 5000              in use — change in Step 2` per D-07
      4. Port 3000: call `is_port_free(3000)` — show ✔ or ✗ with same message
      5. Disk space: call `check_disk_space()` — if >= 5GB show `✔  Disk space  {N} GB free`; if < 5GB show `[#fdcb6e]⏳[/]  Disk space  {N} GB free — 5 GB recommended` per UI-SPEC copywriting
    - Each check result displayed as a `Label` with Rich markup for colored icons
    - Status icon column: 3 cells wide, check name: 22 cells wide, result: remainder — use f-string padding: `f"{'✔':3s}{'Docker Engine':22s}installed"`
    - Store docker_missing and port_conflicts as instance variables for passing to Step 2/3
    - Next Step button: accent class, right-aligned. Enabled after all checks complete.
    - Back button: dim class, left-aligned. Pops back to main menu (Cancel).
    - On Next Step press: `self.app.push_screen(Step2ConfigurationScreen(docker_missing=self.docker_missing, default_api_port=5000, default_frontend_port=3000))`

    **Step2ConfigurationScreen** per UI-SPEC and D-08/D-09:
    - Constructor accepts `docker_missing: bool`, `default_api_port: int`, `default_frontend_port: int`
    - `compose()` yields: `StepIndicator(current_step=2)`, then 5 labeled input groups stacked vertically:
      1. `Label("Install directory")` + `Input(value="~/bitnest", id="install_dir")`
      2. `Label("API port")` + `Input(value=str(default_api_port), id="api_port")`
      3. `Label("Frontend port")` + `Input(value=str(default_frontend_port), id="frontend_port")`
      4. `Label("Admin username  (min 3 characters)")` + `Input(id="admin_user")`
      5. `Label("Admin password  (min 8 characters, hidden)")` + `Input(password=True, id="admin_pass")`
    - Each input followed by a `Label(id="{field}_error", classes="destructive dim")` for inline validation errors (hidden by default)
    - Back button (dim class): `self.app.pop_screen()`
    - "Next Step" button (accent class): triggers `_validate_and_advance()`:
      1. Call `validate_path(install_dir_input.value)` — if invalid, show error label
      2. Call `validate_port(api_port_input.value)` — if invalid, show error label
      3. Call `validate_port(frontend_port_input.value)` — if invalid, show error label
      4. Re-check port conflicts: `is_port_free(int(api_port))` and `is_port_free(int(frontend_port))` per D-07. If conflict, show "Port {N} is in use. Choose a different port." per UI-SPEC copywriting
      5. Call `validate_username(admin_user_input.value)` — if invalid, show error label
      6. Call `validate_password(admin_pass_input.value)` — if invalid, show error label
      7. If ALL valid: collect config dict with keys install_dir (resolved absolute path), api_port, frontend_port, admin_user, admin_pass, db_password (generate_secret()), jwt_key (generate_secret()), docker_missing. Push Step3InstallingScreen(config).
    - Validation errors shown below each field as dim destructive text per UI-SPEC. Hidden when valid. Only triggered on Next Step press, NOT on keystroke per interaction contract.
    - Tab/Shift+Tab moves between inputs per Textual default behavior
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/CsharpProjekte/BitNest && python -c "import ast; ast.parse(open('installers/linux-x86_64/install.py').read()); print('SYNTAX OK')" && python -m pytest installers/linux-x86_64/tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - installers/linux-x86_64/install.py contains `class Step1PrerequisitesScreen(Screen)`
    - installers/linux-x86_64/install.py contains `class Step2ConfigurationScreen(Screen)`
    - installers/linux-x86_64/install.py contains `check_docker()` called within Step1
    - installers/linux-x86_64/install.py contains `is_port_free(` called within Step1
    - installers/linux-x86_64/install.py contains `check_disk_space(` called within Step1
    - installers/linux-x86_64/install.py contains `Input(password=True` for admin password field
    - installers/linux-x86_64/install.py contains `validate_port(` called within Step2 validation
    - installers/linux-x86_64/install.py contains `validate_path(` called within Step2 validation
    - installers/linux-x86_64/install.py contains `validate_username(` called within Step2 validation
    - installers/linux-x86_64/install.py contains `validate_password(` called within Step2 validation
    - installers/linux-x86_64/install.py contains `generate_secret()` called within Step2 config collection
    - installers/linux-x86_64/install.py contains `@work` decorator for Step1 prerequisite checks
    - installers/linux-x86_64/install.py contains `Docker will be installed automatically`
    - installers/linux-x86_64/install.py contains `in use — change in Step 2`
    - File parses without SyntaxError
    - Existing unit tests still pass
  </acceptance_criteria>
  <done>Step1 and Step2 screens implemented with prerequisite checks in @work worker, inline validation on Next Step press, and proper Back/Next navigation. All UI-SPEC copywriting matches.</done>
</task>

</tasks>

<verification>
- `python -c "import ast; ast.parse(open('installers/linux-x86_64/install.py').read())"` — no SyntaxError
- `python -m pytest installers/linux-x86_64/tests/ -x -q` — all tests still pass
- `grep -c "class.*Screen" installers/linux-x86_64/install.py` returns >= 3 (MainMenu, Step1, Step2)
- `grep -c "push_screen" installers/linux-x86_64/install.py` returns >= 3
</verification>

<success_criteria>
- InstallerApp launches with MainMenuScreen
- MainMenu has 3 options, pre-highlights based on state file
- Step1 runs all prerequisite checks asynchronously and displays results
- Step2 collects all 5 config fields with inline validation
- Back/Next navigation works between MainMenu -> Step1 -> Step2
- All existing unit tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/10-linux-x86-64-installer/10-02-SUMMARY.md`
</output>
