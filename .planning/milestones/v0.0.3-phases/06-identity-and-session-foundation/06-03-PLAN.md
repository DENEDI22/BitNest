---
phase: 06-identity-and-session-foundation
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - FrontEnd/index.html
  - FrontEnd/main.js
  - FrontEnd/style.css
  - BitNest.Tests/Auth/AuthFrontendFlowTests.cs
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Unauthenticated users see auth-first entry screen at root URL"
    - "User can sign up and sign in with remember-me option from web UI"
    - "App blocks file area until auth state resolves on startup"
    - "Logout returns user to sign-in and session is not usable afterward"
  artifacts:
    - path: "FrontEnd/index.html"
      provides: "Auth form and app shell structure"
    - path: "FrontEnd/main.js"
      provides: "Auth bootstrap, form submission, session handling"
    - path: "FrontEnd/style.css"
      provides: "Auth screen and loading gate visual states"
  key_links:
    - from: "FrontEnd/main.js"
      to: "/auth/me"
      via: "startup auth bootstrap"
      pattern: "fetch\(`\$\{API_URL\}/auth/me"
    - from: "FrontEnd/main.js"
      to: "/auth/logout"
      via: "logout handler"
      pattern: "fetch\(`\$\{API_URL\}/auth/logout"
---

<objective>
Deliver the locked frontend auth UX: auth-first root screen, remember-me login/signup, startup auth gate, and logout return flow.

Purpose: Complete user-visible phase outcomes by wiring backend auth APIs into existing file UI shell.
Output: Updated frontend HTML/JS/CSS plus a final human verification checkpoint for end-to-end auth behavior.
</objective>

<execution_context>
@/home/danylo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/danylo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-identity-and-session-foundation/06-CONTEXT.md
@.planning/phases/06-identity-and-session-foundation/06-02-SUMMARY.md
@FrontEnd/index.html
@FrontEnd/main.js
@FrontEnd/style.css

<interfaces>
From locked decisions in `06-CONTEXT.md`:
```text
Use dedicated auth-first screen at root URL for unauthenticated users.
Remember me option required.
On startup, block file UI until auth resolves.
On successful logout, show brief confirmation then return to sign-in.
```

Required API calls:
```text
POST /auth/signup
POST /auth/login
POST /auth/logout
POST /auth/refresh
GET /auth/me
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add frontend auth flow tests for bootstrap/login/logout behavior</name>
  <files>BitNest.Tests/Auth/AuthFrontendFlowTests.cs, BitNest.Tests/BitNest.Tests.csproj</files>
  <read_first>FrontEnd/main.js, FrontEnd/index.html, .planning/phases/06-identity-and-session-foundation/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: App startup calls `/auth/me` before showing file list UI.
    - Test 2: Login form sends remember-me flag and switches UI to file screen on success.
    - Test 3: Logout action returns UI to sign-in state and displays short confirmation message.
  </behavior>
  <action>Create `AuthFrontendFlowTests.cs` to validate frontend auth orchestration logic (request order, state transitions, and logout state reset). Use deterministic unit-level tests around extracted JS state helpers or script-invocation wrappers so tests can run in CI without browser automation dependencies.</action>
  <acceptance_criteria>`BitNest.Tests/Auth/AuthFrontendFlowTests.cs` contains test names `Startup_requires_auth_resolution_before_file_ui`, `Login_submits_remember_me_and_enters_app_state`, and `Logout_resets_auth_state_and_shows_confirmation`.</acceptance_criteria>
  <verify>
    <automated>dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~AuthFrontendFlowTests"</automated>
  </verify>
  <done>Frontend auth flow expectations are encoded as executable tests before UI wiring changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auth-first UI and session bootstrap in static frontend</name>
  <files>FrontEnd/index.html, FrontEnd/main.js, FrontEnd/style.css</files>
  <read_first>FrontEnd/index.html, FrontEnd/main.js, FrontEnd/style.css, .planning/phases/06-identity-and-session-foundation/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Unauthenticated root view shows signup/login forms instead of file upload/list sections.
    - Test 2: Successful signup/login transitions directly into existing upload/list/download app screen.
    - Test 3: Refresh failure path returns user to sign-in with clear short inline message.
  </behavior>
  <action>Update `FrontEnd/index.html` to include an auth container (username, password, remember-me, signup/login submit actions, inline error region) and an app container for existing file UI. Update `FrontEnd/main.js` to (a) run startup auth gate via `/auth/me` then `/auth/refresh` fallback, (b) submit signup/login payloads to `/auth/signup` and `/auth/login`, (c) include remember-me boolean in auth payload, (d) guard all protected file actions by checking auth state and handling `401` by returning to auth screen, and (e) call `/auth/logout` then show brief confirmation before sign-in view. Update `FrontEnd/style.css` to style auth card + loading gate while preserving existing visual language and mobile responsiveness.</action>
  <acceptance_criteria>`FrontEnd/main.js` contains fetch calls to `/auth/signup`, `/auth/login`, `/auth/refresh`, `/auth/logout`, and `/auth/me`; `FrontEnd/index.html` contains auth form elements with ids `authUsername`, `authPassword`, and `rememberMe`; `FrontEnd/main.js` contains explicit startup auth gate function invoked before `loadFiles`; `FrontEnd/style.css` contains selectors for auth container and hidden/visible state toggles.</acceptance_criteria>
  <verify>
    <automated>dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~AuthFrontendFlowTests"</automated>
  </verify>
  <done>Frontend supports required auth entry/session behavior while preserving existing post-login file workflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end auth UX from browser</name>
  <action>Run the complete browser verification script after automated implementation and tests finish, then return either approval or concrete defects so execution can proceed or loop on fixes.</action>
  <what-built>Auth-first screen, signup/login/remember-me flow, startup auth gate, refresh fallback, and logout return behavior are implemented in static frontend and wired to backend `/auth/*` endpoints.</what-built>
  <how-to-verify>
    1) Start stack and open the root URL while signed out; verify auth screen appears and file UI is not visible before auth resolves.
    2) Sign up with a new username and password (>=8 chars), then confirm immediate transition to file app screen.
    3) Log out and verify brief confirmation appears and sign-in form returns.
    4) Sign in with remember-me checked; refresh browser tab and confirm session restores without re-login.
    5) In a second tab after logout, attempt a protected file action and confirm it redirects back to sign-in on next action.
  </how-to-verify>
  <acceptance_criteria>All five verification steps complete with expected behavior and no console/runtime errors blocking auth flow.</acceptance_criteria>
  <verify>
    <automated>dotnet build BitNest.sln</automated>
  </verify>
  <done>User confirms the visible auth experience satisfies locked phase decisions and roadmap success criteria.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Automated:
- `dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~AuthFrontendFlowTests"`
- `dotnet build BitNest.sln`

Manual:
- Execute checkpoint browser flow and capture approval/issues.
</verification>

<success_criteria>
- Root URL is auth-first until session resolves.
- Signup/login/logout flows work from the web frontend with inline error handling.
- Remember-me and refresh behaviors are visible and functional.
</success_criteria>

<output>
After completion, create `.planning/phases/06-identity-and-session-foundation/06-03-SUMMARY.md`
</output>
