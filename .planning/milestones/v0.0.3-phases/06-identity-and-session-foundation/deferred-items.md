# Deferred Items

- 2026-03-19: Local full-stack startup for checkpoint automation fails in `BitNest/Program.cs` migration step with `PendingModelChangesWarning` against PostgreSQL. This appears pre-existing to this plan's frontend-only scope, so it was not changed here.
