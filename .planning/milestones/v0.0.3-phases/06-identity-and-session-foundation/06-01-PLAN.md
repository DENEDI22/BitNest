---
phase: 06-identity-and-session-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BitNest/BitNest.csproj
  - BitNest/Data/AppDbContext.cs
  - BitNest/Models/User.cs
  - BitNest/Models/RefreshSession.cs
  - BitNest/Services/PasswordHasher.cs
  - BitNest/Migrations/*AuthFoundation*.cs
  - BitNest/Migrations/AppDbContextModelSnapshot.cs
  - BitNest.Tests/BitNest.Tests.csproj
  - BitNest.Tests/Auth/AuthModelValidationTests.cs
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "System can persist registered users with normalized usernames and password hashes"
    - "Duplicate username attempts can be rejected by persistent uniqueness rules"
  artifacts:
    - path: "BitNest/Models/User.cs"
      provides: "User identity persistence model"
    - path: "BitNest/Models/RefreshSession.cs"
      provides: "Refresh token/session persistence model"
    - path: "BitNest/Data/AppDbContext.cs"
      provides: "DbSet and model constraints for auth entities"
  key_links:
    - from: "BitNest/Data/AppDbContext.cs"
      to: "BitNest/Models/User.cs"
      via: "DbSet<User> and unique index"
      pattern: "HasIndex\(x => x\.NormalizedUsername\)\.IsUnique\(\)"
    - from: "BitNest/Services/PasswordHasher.cs"
      to: "BitNest/Models/User.cs"
      via: "stored hash format"
      pattern: "Verify\(string password, string storedHash\)"
---

<objective>
Create the auth persistence foundation and hashing contract so signup/login APIs can be built safely in the next plan.

Purpose: Establish irreversible security primitives first (hashing + unique identity + refresh-session storage).
Output: Auth entities, DbContext wiring, migration files, and baseline auth test scaffold.
</objective>

<execution_context>
@/home/danylo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/danylo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-identity-and-session-foundation/06-CONTEXT.md
@.planning/phases/06-identity-and-session-foundation/06-RESEARCH.md
@BitNest/Data/AppDbContext.cs
@BitNest/Models/FileMetadata.cs

<interfaces>
From `BitNest/Data/AppDbContext.cs`:
```csharp
public class AppDbContext : DbContext
{
    public DbSet<FileMetadata> Files { get; set; }
    public DbSet<ChunkMetadata> Chunks { get; set; }
    public DbSet<FileChunk> FileChunks { get; set; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create auth test scaffold and model contract tests</name>
  <files>BitNest.Tests/BitNest.Tests.csproj, BitNest.Tests/Auth/AuthModelValidationTests.cs</files>
  <read_first>BitNest/BitNest.csproj, BitNest/Data/AppDbContext.cs, .planning/phases/06-identity-and-session-foundation/06-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Normalizing `Alice.User` yields `alice.user` and passes handle regex `^[a-z0-9._-]+$`.
    - Test 2: Password shorter than 8 chars is rejected by auth validation helper.
    - Test 3: Refresh session cannot be treated active when `RevokedAt` is set or `ExpiresAt` is in the past.
  </behavior>
  <action>Create `BitNest.Tests` xUnit project targeting `net9.0`, reference `BitNest/BitNest.csproj`, and add `Auth/AuthModelValidationTests.cs` with executable tests for username normalization pattern, minimum password length rule (>=8), and refresh-session active-state logic. Keep tests deterministic and runnable with `dotnet test` without network/database containers.</action>
  <acceptance_criteria>`BitNest.Tests/BitNest.Tests.csproj` contains `TargetFramework>net9.0</TargetFramework>` and `ProjectReference Include="..\BitNest\BitNest.csproj"`; `BitNest.Tests/Auth/AuthModelValidationTests.cs` contains test names `Username_is_normalized_to_lowercase_handle`, `Password_must_be_at_least_8_characters`, and `Refresh_session_is_inactive_when_revoked_or_expired`.</acceptance_criteria>
  <verify>
    <automated>dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~AuthModelValidationTests"</automated>
  </verify>
  <done>Auth-focused test harness exists and passes locally, creating a stable baseline for later auth implementation plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add User and RefreshSession models with password hash service</name>
  <files>BitNest/Models/User.cs, BitNest/Models/RefreshSession.cs, BitNest/Services/PasswordHasher.cs, BitNest/Data/AppDbContext.cs</files>
  <read_first>BitNest/Models/FileMetadata.cs, BitNest/Models/FileChunk.cs, BitNest/Data/AppDbContext.cs, BitNest/Services/StorageService.cs, .planning/phases/06-identity-and-session-foundation/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: New user records store `NormalizedUsername` lowercase and keep original `Username` display value.
    - Test 2: `PasswordHasher.Hash` never returns plaintext and `Verify` succeeds only for matching password.
    - Test 3: RefreshSession includes `TokenHash`, `ExpiresAt`, `RevokedAt`, `ReplacedBySessionId`, and `RememberMe` fields used by rotation flow.
  </behavior>
  <action>Create `User` and `RefreshSession` entity classes in `BitNest/Models` and wire them in `AppDbContext` via `DbSet<User>` and `DbSet<RefreshSession>`. In `OnModelCreating`, add a unique index on `User.NormalizedUsername`, configure relationship `RefreshSession.UserId -> User.Id`, and index `RefreshSession` by `(UserId, ExpiresAt)`. Add `BitNest/Services/PasswordHasher.cs` with methods `string Hash(string password)` and `bool Verify(string password, string storedHash)` using `Rfc2898DeriveBytes` with explicit iterations and salt/version prefix in stored hash format.</action>
  <acceptance_criteria>`BitNest/Data/AppDbContext.cs` contains `DbSet<User>` and `DbSet<RefreshSession>` plus `HasIndex(x => x.NormalizedUsername).IsUnique()`; `BitNest/Services/PasswordHasher.cs` exports `Hash(` and `Verify(` methods; `BitNest/Models/RefreshSession.cs` contains properties `TokenHash`, `ExpiresAt`, `RevokedAt`, and `RememberMe`.</acceptance_criteria>
  <verify>
    <automated>dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~AuthModelValidationTests"</automated>
  </verify>
  <done>Auth domain entities and hash/verify primitives exist and are aligned with locked decision constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Generate EF Core migration for auth foundation schema</name>
  <files>BitNest/Migrations/*AuthFoundation*.cs, BitNest/Migrations/AppDbContextModelSnapshot.cs</files>
  <read_first>BitNest/Data/AppDbContext.cs, BitNest/Migrations/AppDbContextModelSnapshot.cs</read_first>
  <action>Run EF migration command from `BitNest/` to create a new migration named `AuthFoundation` that adds `Users` and `RefreshSessions` tables, unique index for normalized username, and foreign key from refresh sessions to users. Keep existing file/chunk schema unchanged except snapshot updates required by EF.</action>
  <acceptance_criteria>Migration files contain `CreateTable("Users"` and `CreateTable("RefreshSessions"`; migration contains unique index on `NormalizedUsername`; snapshot file includes `modelBuilder.Entity("BitNest.Models.User"` and `modelBuilder.Entity("BitNest.Models.RefreshSession"`.</acceptance_criteria>
  <verify>
    <automated>dotnet build BitNest.sln</automated>
  </verify>
  <done>Database migration is ready and build passes with new auth models integrated.</done>
</task>

</tasks>

<verification>
Run auth model test slice and full solution build:
- `dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~AuthModelValidationTests"`
- `dotnet build BitNest.sln`
</verification>

<success_criteria>
- User and refresh session entities exist with required constraints.
- Password hashing/verification service is implemented and covered by auth model tests.
- EF migration compiles and adds auth tables/indexes without regressing existing storage schema.
</success_criteria>

<output>
After completion, create `.planning/phases/06-identity-and-session-foundation/06-01-SUMMARY.md`
</output>
