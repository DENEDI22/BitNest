---
phase: 07-user-management-and-file-access-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BitNest.Tests/Auth/AdminUserEndpointTests.cs
  - BitNest.Tests/Storage/AccessControlTests.cs
  - BitNest/Models/User.cs
  - BitNest/Models/FileMetadata.cs
  - BitNest/Models/FileGrant.cs
  - BitNest/Data/AppDbContext.cs
  - BitNest/Migrations/*_Phase7AccessFoundation.cs
  - BitNest/Migrations/AppDbContextModelSnapshot.cs
autonomous: true
requirements: [USER-02, ACCS-05]
must_haves:
  truths:
    - "System persists admin and active-status flags per user."
    - "System persists per-file grant records linking owner-managed access to users."
    - "Storage records have owner identity available for authorization checks."
  artifacts:
    - path: "BitNest/Models/FileGrant.cs"
      provides: "Per-file grant persistence entity"
    - path: "BitNest/Data/AppDbContext.cs"
      provides: "EF relationships and indexes for owner/grant model"
    - path: "BitNest/Migrations/*_Phase7AccessFoundation.cs"
      provides: "Database migration for user role/status and file grant tables"
  key_links:
    - from: "BitNest/Models/FileMetadata.cs"
      to: "BitNest/Models/User.cs"
      via: "OwnerUserId foreign key"
      pattern: "OwnerUserId"
    - from: "BitNest/Models/FileGrant.cs"
      to: "BitNest/Models/FileMetadata.cs"
      via: "FileId foreign key"
      pattern: "FileId"
    - from: "BitNest/Models/FileGrant.cs"
      to: "BitNest/Models/User.cs"
      via: "GrantedUserId and GrantedByUserId"
      pattern: "GrantedUserId|GrantedByUserId"
---

<objective>
Create the Phase 7 persistence foundation for admin controls and file grant authorization.

Purpose: Backend authorization and admin APIs in later plans need concrete schema support for role/status, ownership, and per-file grants.
Output: New/updated EF entities, migration artifacts, and failing-then-passing contract tests that lock schema intent.
</objective>

<execution_context>
@/home/danylo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/danylo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-user-management-and-file-access-enforcement/07-CONTEXT.md
@.planning/phases/07-user-management-and-file-access-enforcement/07-RESEARCH.md
@BitNest/Data/AppDbContext.cs
@BitNest/Models/User.cs
@BitNest/Models/FileMetadata.cs

<interfaces>
From `BitNest/Models/User.cs`:
```csharp
public class User
{
    public int Id { get; set; }
    public string Username { get; set; }
    public string NormalizedUsername { get; set; }
    public string PasswordHash { get; set; }
    public DateTime CreatedAt { get; set; }
    public List<RefreshSession> RefreshSessions { get; set; }
}
```

From `BitNest/Models/FileMetadata.cs`:
```csharp
public class FileMetadata
{
    public int Id { get; set; }
    public string Name { get; set; }
    public bool IsDeleted { get; set; }
    public List<FileChunk> Chunks { get; set; }
}
```

From `BitNest/Data/AppDbContext.cs`:
```csharp
public DbSet<FileMetadata> Files { get; set; }
public DbSet<User> Users { get; set; }
protected override void OnModelCreating(ModelBuilder modelBuilder) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing persistence contract tests for role/status and grant schema</name>
  <files>BitNest.Tests/Auth/AdminUserEndpointTests.cs, BitNest.Tests/Storage/AccessControlTests.cs</files>
  <read_first>BitNest.Tests/Auth/AuthEndpointTests.cs, BitNest/Data/AppDbContext.cs, BitNest/Models/User.cs, BitNest/Models/FileMetadata.cs</read_first>
  <behavior>
    - Test 1: Access-control seed setup can create a file owner and a granted user through explicit model fields.
    - Test 2: User disable flow expects persisted inactive status and revocable sessions as schema-supported properties.
    - Test 3: Duplicate grant for same file+user is rejected by unique constraint expectation.
  </behavior>
  <action>Create new xUnit files `AdminUserEndpointTests.cs` and `AccessControlTests.cs` with RED tests that assert the required schema contracts before implementation: `User.IsAdmin`, `User.IsActive`, `User.LastSignInAt`, `FileMetadata.OwnerUserId`, and unique file-grant semantics on `(FileId, GrantedUserId)`. Keep tests integration-style using TestServer + EF InMemory patterns from `AuthEndpointTests` and name methods with requirement intent (`..._for_OWNER_and_GRANTED_users`, `..._marks_user_inactive...`, etc.).</action>
  <acceptance_criteria>`BitNest.Tests/Auth/AdminUserEndpointTests.cs` contains `class AdminUserEndpointTests`; `BitNest.Tests/Storage/AccessControlTests.cs` contains `class AccessControlTests`; both files contain at least one `[Fact]` asserting owner/grant or disable-schema expectations.</acceptance_criteria>
  <verify>
    <automated>~/.dotnet/dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~(AdminUserEndpointTests|AccessControlTests)"</automated>
  </verify>
  <done>Focused phase-7 test scaffolds exist and fail only for missing schema/model behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement role/status/ownership/grant models and migration</name>
  <files>BitNest/Models/User.cs, BitNest/Models/FileMetadata.cs, BitNest/Models/FileGrant.cs, BitNest/Data/AppDbContext.cs, BitNest/Migrations/*_Phase7AccessFoundation.cs, BitNest/Migrations/AppDbContextModelSnapshot.cs</files>
  <read_first>BitNest/Models/User.cs, BitNest/Models/FileMetadata.cs, BitNest/Data/AppDbContext.cs, BitNest/Migrations/20260319003427_AuthFoundation.cs, BitNest/Migrations/AppDbContextModelSnapshot.cs</read_first>
  <behavior>
    - Test 1: EF model includes file owner and grant links with valid foreign keys.
    - Test 2: User entity exposes admin/active/last-sign-in fields used by later plan APIs.
    - Test 3: Migration introduces grant table and required indexes/constraints without dropping existing auth/storage tables.
  </behavior>
  <action>Update `User` model with `IsAdmin` (default false), `IsActive` (default true), `LastSignInAt` (nullable), plus grant/owned-file navigation collections. Update `FileMetadata` with `OwnerUserId` and `OwnerUser` navigation plus grant collection. Create `FileGrant` model with `Id`, `FileId`, `GrantedUserId`, `GrantedByUserId`, `CreatedAt` and matching navigations. In `AppDbContext`, add `DbSet<FileGrant>` and configure: unique index on `{ FileId, GrantedUserId }`; FK `FileMetadata.OwnerUserId -> User.Id`; FK `FileGrant.FileId -> FileMetadata.Id` cascade; FK `FileGrant.GrantedUserId -> User.Id` restrict/cascade-safe; FK `FileGrant.GrantedByUserId -> User.Id` restrict/cascade-safe. Generate migration named `Phase7AccessFoundation` and ensure snapshot is updated.</action>
  <acceptance_criteria>`BitNest/Models/User.cs` contains `IsAdmin` and `IsActive`; `BitNest/Models/FileMetadata.cs` contains `OwnerUserId`; `BitNest/Models/FileGrant.cs` exists with `GrantedUserId` and `GrantedByUserId`; `BitNest/Data/AppDbContext.cs` contains `DbSet<FileGrant>` and `HasIndex(x => new { x.FileId, x.GrantedUserId }).IsUnique()`; a migration file with suffix `_Phase7AccessFoundation.cs` exists.</acceptance_criteria>
  <verify>
    <automated>~/.dotnet/dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~(AdminUserEndpointTests|AccessControlTests)"</automated>
  </verify>
  <done>Schema, model configuration, and migration artifacts compile and satisfy new persistence contracts for USER-02 and ACCS-05.</done>
</task>

</tasks>

<verification>
Run focused phase-7 scaffolding tests and confirm migration artifacts are present in `BitNest/Migrations/`.
</verification>

<success_criteria>
Database schema supports role/status and per-file grants, with test scaffolds enforcing these contracts for downstream authorization and admin plans.
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-management-and-file-access-enforcement/07-01-SUMMARY.md`
</output>
