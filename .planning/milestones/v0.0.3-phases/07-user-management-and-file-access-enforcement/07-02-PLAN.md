---
phase: 07-user-management-and-file-access-enforcement
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - BitNest.Tests/Auth/AdminUserEndpointTests.cs
  - BitNest.Tests/Storage/AccessControlTests.cs
  - BitNest/Controllers/AuthController.cs
  - BitNest/Controllers/StorageController.cs
  - BitNest/Controllers/AdminUsersController.cs
  - BitNest/Services/AuthService.cs
  - BitNest/Services/StorageService.cs
  - BitNest/DTOs/Auth/MeResponseDto.cs
  - BitNest/DTOs/Admin/AdminUserListItemDto.cs
  - BitNest/DTOs/Admin/AdminCreateUserRequestDto.cs
  - BitNest/DTOs/Admin/AdminDisableUserResponseDto.cs
  - BitNest/DTOs/Storage/AuthorizedFileMetadataDto.cs
  - BitNest/Program.cs
autonomous: true
requirements: [USER-01, USER-02, USER-03, ACCS-01, ACCS-02, ACCS-03]
must_haves:
  truths:
    - "Admin APIs allow listing users, creating users, and disabling users with active-session invalidation."
    - "File list endpoint returns only files owned by caller or explicitly granted."
    - "Download/delete endpoints deny unauthorized access via unified 404 behavior."
  artifacts:
    - path: "BitNest/Controllers/AdminUsersController.cs"
      provides: "Admin-only user-management API endpoints"
    - path: "BitNest/Services/StorageService.cs"
      provides: "Owner/grant authorization checks for list/download/delete"
    - path: "BitNest/Services/AuthService.cs"
      provides: "Disabled-user guard for login/refresh and last-sign-in updates"
  key_links:
    - from: "BitNest/Controllers/StorageController.cs"
      to: "BitNest/Services/StorageService.cs"
      via: "authorized metadata and file action checks"
      pattern: "NotFound|CanAccess"
    - from: "BitNest/Controllers/AdminUsersController.cs"
      to: "BitNest/Data/AppDbContext.cs"
      via: "admin user CRUD through service layer"
      pattern: "Users"
    - from: "BitNest/Services/AuthService.cs"
      to: "BitNest/Models/User.cs"
      via: "IsActive and LastSignInAt logic"
      pattern: "IsActive|LastSignInAt"
---

<objective>
Implement backend authorization and admin user-management contracts on top of the phase-7 persistence model.

Purpose: This plan enforces all server-side guarantees for resource visibility and admin actions so frontend behavior is driven by trusted backend rules.
Output: New admin API surface, updated auth/session behavior for disabled users, and storage authorization enforcement for list/download/delete.
</objective>

<execution_context>
@/home/danylo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/danylo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-user-management-and-file-access-enforcement/07-CONTEXT.md
@.planning/phases/07-user-management-and-file-access-enforcement/07-RESEARCH.md
@.planning/phases/07-user-management-and-file-access-enforcement/07-01-SUMMARY.md
@BitNest/Controllers/AuthController.cs
@BitNest/Controllers/StorageController.cs
@BitNest/Services/AuthService.cs
@BitNest/Services/StorageService.cs

<interfaces>
From `BitNest/Controllers/StorageController.cs`:
```csharp
[HttpGet("download/{fileId}")]
public async Task<ActionResult> Download(int fileId)

[HttpGet("{pageNumber}")]
public async Task<IActionResult> Files(int pageNumber = 1)

[HttpDelete("{fileId}")]
public async Task<IActionResult> DeleteFile(int fileId)
```

From `BitNest/Services/AuthService.cs`:
```csharp
public async Task<ServiceResult<AuthTokensDto>> Login(AuthCredentialRequestDto request)
public async Task<ServiceResult<AuthTokensDto>> Refresh(RefreshRequestDto request)
public async Task<ServiceResult<MeResponseDto>> GetMe(int userId)
```

From `BitNest/DTOs/Auth/MeResponseDto.cs`:
```csharp
public class MeResponseDto
{
    public int Id { get; set; }
    public string Username { get; set; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand backend contract tests for admin endpoints and storage authorization outcomes</name>
  <files>BitNest.Tests/Auth/AdminUserEndpointTests.cs, BitNest.Tests/Storage/AccessControlTests.cs</files>
  <read_first>BitNest.Tests/Auth/AuthEndpointTests.cs, BitNest/Controllers/StorageController.cs, BitNest/Services/StorageService.cs, BitNest/Services/AuthService.cs</read_first>
  <behavior>
    - Test 1: Non-admin calls to admin endpoints return 403; admin calls succeed.
    - Test 2: Disabling a user prevents subsequent refresh/login and invalidates existing refresh token usage.
    - Test 3: File list includes owner + granted files only; unauthorized file download/delete returns 404.
  </behavior>
  <action>Update/create integration tests so they assert required status codes and payloads for `/admin/users`, `/admin/users/{id}/disable`, `/Storage/{page}`, `/Storage/download/{fileId}`, and `DELETE /Storage/{fileId}`. Include explicit assertions for unified 404 response on unauthorized file access and session invalidation behavior after admin disable. Keep tests deterministic with in-memory DB seeding for owner, granted user, and unrelated user.</action>
  <acceptance_criteria>`BitNest.Tests/Auth/AdminUserEndpointTests.cs` contains assertions for 403 non-admin and successful admin list/create/disable flows; `BitNest.Tests/Storage/AccessControlTests.cs` contains assertions for 404 unauthorized download/delete and filtered file list behavior.</acceptance_criteria>
  <verify>
    <automated>~/.dotnet/dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~(AdminUserEndpointTests|AccessControlTests)"</automated>
  </verify>
  <done>Test suite captures full backend behavior for USER-01/02/03 and ACCS-01/02/03 before implementation changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admin APIs, disabled-user auth guards, and storage owner/grant enforcement</name>
  <files>BitNest/Controllers/AdminUsersController.cs, BitNest/Controllers/AuthController.cs, BitNest/Controllers/StorageController.cs, BitNest/Services/AuthService.cs, BitNest/Services/StorageService.cs, BitNest/DTOs/Auth/MeResponseDto.cs, BitNest/DTOs/Admin/AdminUserListItemDto.cs, BitNest/DTOs/Admin/AdminCreateUserRequestDto.cs, BitNest/DTOs/Admin/AdminDisableUserResponseDto.cs, BitNest/DTOs/Storage/AuthorizedFileMetadataDto.cs, BitNest/Program.cs</files>
  <read_first>BitNest/Controllers/AuthController.cs, BitNest/Controllers/StorageController.cs, BitNest/Services/AuthService.cs, BitNest/Services/StorageService.cs, BitNest/Program.cs, BitNest/DTOs/Auth/AuthErrorDto.cs</read_first>
  <behavior>
    - Test 1: Storage list query returns only owner/granted records; hidden unauthorized files never appear.
    - Test 2: Unauthorized direct file access returns 404 with unified not-found message body, not 403.
    - Test 3: Admin create/list/disable endpoints require admin privileges and disabling revokes user sessions immediately.
  </behavior>
  <action>Add `AdminUsersController` with `[Authorize]` endpoints: `GET /admin/users`, `POST /admin/users`, `POST /admin/users/{id}/disable`. Enforce admin checks using current user identity from JWT and `User.IsAdmin`; return 403 for non-admin calls. In `AuthService`, reject disabled users in `Login` and `Refresh`, update `LastSignInAt` on successful login, and include `IsAdmin`/`IsActive` on `MeResponseDto`. On disable action, set target `User.IsActive=false` and revoke all active `RefreshSessions` for that user in same transaction. In `StorageService`, implement owner/grant authorization query helpers and update list/download/delete methods to require owner or grant access; when unauthorized or missing, surface identical 404 behavior (including the same message text payload key). In `StorageController`, require authorization for list/download/delete, pass current user id into service methods, and map unauthorized resource access to unified `NotFound(...)`. Register any new services in `Program.cs` and keep controller thin-service style.</action>
  <acceptance_criteria>`BitNest/Controllers/AdminUsersController.cs` exists with `/admin/users` routes; `BitNest/Services/AuthService.cs` contains `IsActive` checks in `Login` and `Refresh`; `BitNest/DTOs/Auth/MeResponseDto.cs` includes admin/status fields; `BitNest/Services/StorageService.cs` contains owner/grant query conditions; `BitNest/Controllers/StorageController.cs` applies `[Authorize]` and returns unified 404 for denied file targets.</acceptance_criteria>
  <verify>
    <automated>~/.dotnet/dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~(AdminUserEndpointTests|AccessControlTests)"</automated>
  </verify>
  <done>Backend reliably enforces authorization and admin control behaviors required by USER-01/02/03 and ACCS-01/02/03.</done>
</task>

</tasks>

<verification>
Run focused admin/access tests, then run full backend test suite to confirm auth and storage regressions are avoided.
</verification>

<success_criteria>
Server guarantees are in place: only authorized files are visible/actionable, unauthorized direct file actions return unified 404, and admins can manage user lifecycle with immediate disable-session impact.
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-management-and-file-access-enforcement/07-02-SUMMARY.md`
</output>
