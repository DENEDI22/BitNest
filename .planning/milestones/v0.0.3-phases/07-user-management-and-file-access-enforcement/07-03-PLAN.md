---
phase: 07-user-management-and-file-access-enforcement
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - BitNest.Tests/Auth/AuthFrontendFlowTests.cs
  - BitNest.Tests/Auth/FrontendAccessFlowTests.cs
  - FrontEnd/index.html
  - FrontEnd/main.js
  - FrontEnd/style.css
autonomous: false
requirements: [USER-01, USER-03, ACCS-04]
must_haves:
  truths:
    - "Admin users can navigate to `/admin` and manage users from the web UI."
    - "Non-admin users do not see admin entry points and see admin access-denied view on direct `/admin` access."
    - "File UI only surfaces authorized rows/actions, with unified file-404 page for missing/deleted/unauthorized direct actions."
  artifacts:
    - path: "FrontEnd/main.js"
      provides: "Route-aware admin/files/error-page flow and access-aware file actions"
    - path: "FrontEnd/index.html"
      provides: "Admin, access-denied, and unified file-404 view containers"
    - path: "BitNest.Tests/Auth/FrontendAccessFlowTests.cs"
      provides: "Automated assertions for frontend authorization behavior"
  key_links:
    - from: "FrontEnd/main.js"
      to: "/auth/me"
      via: "startup role detection for admin entry visibility"
      pattern: "auth/me"
    - from: "FrontEnd/main.js"
      to: "/admin/users"
      via: "admin list/create/disable requests"
      pattern: "admin/users"
    - from: "FrontEnd/main.js"
      to: "FrontEnd/index.html"
      via: "view toggle for unified file-404 and access-denied pages"
      pattern: "Back to files"
---

<objective>
Implement the Phase 7 frontend experience for admin management and access-aware file workflows.

Purpose: Convert backend authorization guarantees into user-visible behavior while honoring locked UX decisions around route gating and unified error handling.
Output: Updated frontend route logic, admin UI, unified file 404/access-denied pages, and automated frontend behavior checks.
</objective>

<execution_context>
@/home/danylo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/danylo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-user-management-and-file-access-enforcement/07-CONTEXT.md
@.planning/phases/07-user-management-and-file-access-enforcement/07-RESEARCH.md
@.planning/phases/07-user-management-and-file-access-enforcement/07-02-SUMMARY.md
@FrontEnd/index.html
@FrontEnd/main.js
@FrontEnd/style.css

<interfaces>
From `FrontEnd/main.js`:
```javascript
async function bootstrapAuthGate() { ... }
async function ensureAuthenticatedForAction() { ... }
async function loadFiles(page) { ... }
async function downloadFile(fileId) { ... }
async function deleteFile(fileId) { ... }
```

From `FrontEnd/index.html`:
```html
<section class="app-shell view-hidden" id="appContainer"></section>
<ul id="fileList" aria-live="polite"></ul>
```

From phase-7 backend contracts:
```text
GET /auth/me -> includes isAdmin and isActive fields
GET /admin/users, POST /admin/users, POST /admin/users/{id}/disable
Storage unauthorized/missing/deleted direct file target -> unified 404 behavior
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add frontend behavior tests for admin routing and unified access outcomes</name>
  <files>BitNest.Tests/Auth/AuthFrontendFlowTests.cs, BitNest.Tests/Auth/FrontendAccessFlowTests.cs</files>
  <read_first>BitNest.Tests/Auth/AuthFrontendFlowTests.cs, FrontEnd/main.js, FrontEnd/index.html</read_first>
  <behavior>
    - Test 1: Script contains role-aware admin entry gating tied to `/auth/me` identity payload.
    - Test 2: Script includes unified file-404 route/view handling for unauthorized/missing file actions.
    - Test 3: Script contains non-admin `/admin` access-denied route handling and back-to-files action wiring.
  </behavior>
  <action>Create `FrontendAccessFlowTests.cs` with source-contract assertions (string/regex checks) for admin-route visibility, unified file-404 flow, and access-denied route behavior. Extend `AuthFrontendFlowTests.cs` where needed so startup assertions also cover role metadata consumption (`isAdmin`) and route bootstrap ordering when path is `/admin`.</action>
  <acceptance_criteria>`BitNest.Tests/Auth/FrontendAccessFlowTests.cs` exists and contains `[Fact]` methods for admin visibility and unified 404 flow; `BitNest.Tests/Auth/AuthFrontendFlowTests.cs` includes assertions referencing `isAdmin` or admin route bootstrap logic.</acceptance_criteria>
  <verify>
    <automated>~/.dotnet/dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~(AuthFrontendFlowTests|FrontendAccessFlowTests)"</automated>
  </verify>
  <done>Frontend behavior contracts are explicitly test-defined before UI/routing code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `/admin` UI, access-denied page, and unified file 404 flow in frontend</name>
  <files>FrontEnd/index.html, FrontEnd/main.js, FrontEnd/style.css</files>
  <read_first>FrontEnd/index.html, FrontEnd/main.js, FrontEnd/style.css, .planning/phases/07-user-management-and-file-access-enforcement/07-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Non-admin users never see admin nav and direct `/admin` path shows admin access-denied page.
    - Test 2: Admin users can view list/create/disable controls for users on `/admin`.
    - Test 3: Unauthorized/missing/deleted file actions route to dedicated unified 404 page that keeps session active and offers "Back to files".
  </behavior>
  <action>Update `index.html` with dedicated view containers for: admin page (`/admin`), admin access-denied page, and unified file 404 page. Add primary buttons labeled "Back to files" on both error views. In `main.js`, implement lightweight path-based routing (files default, admin route, error routes), consume `/auth/me` response fields to determine admin visibility, and hide admin navigation entirely for non-admin users. Add admin API handlers for list/create/disable users against `/admin/users` endpoints. For file actions (`downloadFile`, `deleteFile`) and direct route handling, when backend returns unified 404, route to unified file 404 view while preserving auth session state. Keep existing auth-expiry behavior for 401 responses unchanged. When file metadata indicates delete disallowed, render Delete button as disabled for that row. Update `style.css` to style new admin and error views consistent with existing theme.</action>
  <acceptance_criteria>`FrontEnd/index.html` contains sections or containers for admin route, admin access-denied, and unified file-404 page; `FrontEnd/main.js` contains route handling for `/admin` and a unified file-404 view function; `FrontEnd/main.js` contains logic that hides admin entry for non-admin users and includes button text `Back to files`; `FrontEnd/main.js` sets `deleteButton.disabled = true` for non-allowed delete scenarios.</acceptance_criteria>
  <verify>
    <automated>~/.dotnet/dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~(AuthFrontendFlowTests|FrontendAccessFlowTests)"</automated>
  </verify>
  <done>Frontend now exposes required admin and authorization-aware UI states while preserving existing auth-session semantics.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify admin and access-control UX in browser</name>
  <what-built>Frontend route and view behavior for admin area, access-denied handling, and unified file 404 flow after backend authorization changes.</what-built>
  <action>Run the focused frontend automated test command first, then execute manual browser verification across admin and non-admin flows to confirm locked decision fidelity (hidden admin entry for non-admins, dedicated access-denied view for `/admin`, and unified file-404 behavior for unauthorized/missing file targets).</action>
  <how-to-verify>
    1) Start app stack (`docker compose up --build`) and sign in as admin.
    2) Confirm admin navigation is visible, open `/admin`, list users, create one user, disable one user.
    3) Sign in as non-admin; confirm admin navigation is hidden and direct `/admin` shows access-denied page with `Back to files` action.
    4) Trigger unauthorized direct file action (or missing file id) and confirm unified file-404 page appears with `Back to files` and session remains signed in.
    5) Return to files and confirm only authorized files/actions are shown.
  </how-to-verify>
  <verify>
    <automated>~/.dotnet/dotnet test BitNest.Tests/BitNest.Tests.csproj --filter "FullyQualifiedName~(AuthFrontendFlowTests|FrontendAccessFlowTests)"</automated>
  </verify>
  <done>Human verification confirms admin-route gating and unified file-404/access-denied flows match context decisions without regressions.</done>
  <acceptance_criteria>Manual flow confirms all five steps above, with no unexpected sign-out during unified file-404 recovery path.</acceptance_criteria>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Run focused frontend behavior tests, then perform browser checkpoint covering admin and authorization UX paths end-to-end.
</verification>

<success_criteria>
Web UI enforces admin visibility rules, supports admin user-management actions, and renders only authorization-appropriate file states with locked unified-404 and access-denied behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-management-and-file-access-enforcement/07-03-SUMMARY.md`
</output>
