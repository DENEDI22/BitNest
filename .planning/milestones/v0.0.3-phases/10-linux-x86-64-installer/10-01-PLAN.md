---
phase: 10-linux-x86-64-installer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BitNest/Program.cs
  - BitNest/Services/AuthService.cs
  - installers/linux-x86_64/install.py
  - installers/linux-x86_64/tests/__init__.py
  - installers/linux-x86_64/tests/conftest.py
  - installers/linux-x86_64/tests/test_validation.py
  - installers/linux-x86_64/tests/test_preflight.py
  - installers/linux-x86_64/tests/test_secrets.py
  - installers/linux-x86_64/tests/test_compose_template.py
  - installers/linux-x86_64/tests/test_state.py
  - installers/linux-x86_64/tests/test_distro.py
  - installers/linux-x86_64/tests/test_health.py
  - installers/linux-x86_64/tests/test_filesystem.py
autonomous: true
requirements:
  - INST-03
  - INST-04
  - INST-05
  - INST-06
  - INST-08
  - INST-09
  - INST-10
  - LINUX-01
  - LINUX-02

must_haves:
  truths:
    - "API creates admin account from env vars on first startup when users table is empty"
    - "Compose template renders valid YAML with pg_isready healthcheck and service_healthy condition"
    - ".env writer produces chmod 600 file with all required env vars"
    - "Port pre-flight correctly detects occupied and free ports"
    - "Distro detection maps debian/ubuntu to apt, fedora to dnf, arch to pacman, unknown to fallback"
    - "State file writes and reads round-trip correctly at XDG config path"
    - "Secret generation produces 64-char hex-only strings"
    - "Health poll parses docker compose ps JSON output correctly"
  artifacts:
    - path: "BitNest/Program.cs"
      provides: "Admin seed logic on startup"
      contains: "BITNEST_ADMIN_USER"
    - path: "installers/linux-x86_64/install.py"
      provides: "Core installer logic functions"
      min_lines: 200
    - path: "installers/linux-x86_64/tests/test_compose_template.py"
      provides: "Compose template unit tests"
      min_lines: 30
    - path: "installers/linux-x86_64/tests/test_distro.py"
      provides: "Distro detection unit tests"
      min_lines: 30
  key_links:
    - from: "BitNest/Program.cs"
      to: "BitNest/Services/AuthService.cs"
      via: "CreateUserAsAdminAsync call"
      pattern: "CreateUserAsAdminAsync"
    - from: "installers/linux-x86_64/install.py"
      to: "compose.yaml template"
      via: "COMPOSE_TEMPLATE.format()"
      pattern: "COMPOSE_TEMPLATE"
---

<objective>
API admin seeding integration + installer core logic functions + full test scaffold.

Purpose: Establish the API admin-seed integration point (D-10) and all pure-function installer logic that the Textual TUI screens will call. This separates testable business logic from UI so that unit tests cover validation, secrets, compose template rendering, state file I/O, distro detection, health polling, and Docker install command selection — all without needing a terminal.

Output: Modified API startup with admin seeding; `installers/linux-x86_64/install.py` with all core functions; full pytest test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-linux-x86-64-installer/10-CONTEXT.md
@.planning/phases/10-linux-x86-64-installer/10-RESEARCH.md
@.planning/phases/10-linux-x86-64-installer/10-VALIDATION.md

<interfaces>
<!-- Existing API code the executor needs -->

From BitNest/Program.cs (lines 86-92 — migration block where admin seed goes):
```csharp
using (var scope = app.Services.CreateScope())
{
    var db = scope.ServiceProvider.GetRequiredService<AppDbContext>();
    db.Database.Migrate();
}
```

From BitNest/Services/AuthService.cs (line 164 — existing admin create method):
```csharp
public async Task<ServiceResult<User>> CreateUserAsAdminAsync(string username, string password, bool isAdmin)
```

From BitNest/Models/User.cs:
```csharp
public class User
{
    // ...
    public bool IsAdmin { get; set; }
}
```

From BitNest/appsettings.json — env var mapping:
```
ConnectionStrings__DefaultConnection  → Npgsql connection string
Auth__SigningKey                       → JWT signing key
UploadsPath                           → /app/data/storage
ASPNETCORE_ENVIRONMENT                → Production
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API admin seeding on startup</name>
  <files>BitNest/Program.cs, BitNest/Services/AuthService.cs</files>
  <read_first>
    BitNest/Program.cs
    BitNest/Services/AuthService.cs
    BitNest/Models/User.cs
    BitNest/Data/AppDbContext.cs
  </read_first>
  <behavior>
    - When BITNEST_ADMIN_USER and BITNEST_ADMIN_PASS env vars are both set AND db.Users table is empty, create admin user with IsAdmin=true
    - When BITNEST_ADMIN_USER is unset or empty, skip seeding silently (no error)
    - When BITNEST_ADMIN_PASS is unset or empty, skip seeding silently (no error)
    - When db.Users table already has records, skip seeding even if env vars are set
    - Admin user created via existing AuthService.CreateUserAsAdminAsync(username, password, isAdmin: true)
  </behavior>
  <action>
    Modify `BitNest/Program.cs` to add admin seeding logic AFTER the migration block (lines 86-92) and BEFORE `app.Run()`. The seeding block must:

    1. Read env vars: `Environment.GetEnvironmentVariable("BITNEST_ADMIN_USER")` and `Environment.GetEnvironmentVariable("BITNEST_ADMIN_PASS")`
    2. If both are non-null and non-empty, create a new scope, resolve `AuthService`, check `!await db.Users.AnyAsync()`, then call `await authService.CreateUserAsAdminAsync(adminUser, adminPass, isAdmin: true)`
    3. Log the result: `Log.Information("Admin account '{Username}' created from environment variables.", adminUser)` on success; `Log.Warning(...)` on failure (e.g. validation error)
    4. If either env var is missing, do nothing (no log, no error)

    Exact code to insert after the migration `using` block:

    ```csharp
    // Seed admin account from environment variables (installer integration)
    {
        var adminUser = Environment.GetEnvironmentVariable("BITNEST_ADMIN_USER");
        var adminPass = Environment.GetEnvironmentVariable("BITNEST_ADMIN_PASS");
        if (!string.IsNullOrEmpty(adminUser) && !string.IsNullOrEmpty(adminPass))
        {
            using var seedScope = app.Services.CreateScope();
            var seedDb = seedScope.ServiceProvider.GetRequiredService<AppDbContext>();
            if (!await seedDb.Users.AnyAsync())
            {
                var authService = seedScope.ServiceProvider.GetRequiredService<AuthService>();
                var result = await authService.CreateUserAsAdminAsync(adminUser, adminPass, isAdmin: true);
                if (result.IsSuccess)
                    Log.Information("Admin account '{Username}' seeded from environment variables.", adminUser);
                else
                    Log.Warning("Failed to seed admin account: {Error}", result.ErrorMessage);
            }
        }
    }
    ```

    Note: `Program.Main` must become `async Task Main` (change `public static void Main` to `public static async Task Main`). Per D-10.
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/CsharpProjekte/BitNest && dotnet build BitNest/BitNest.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BitNest/Program.cs contains `GetEnvironmentVariable("BITNEST_ADMIN_USER")`
    - BitNest/Program.cs contains `GetEnvironmentVariable("BITNEST_ADMIN_PASS")`
    - BitNest/Program.cs contains `CreateUserAsAdminAsync`
    - BitNest/Program.cs contains `!await seedDb.Users.AnyAsync()`
    - BitNest/Program.cs contains `async Task Main`
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>API reads BITNEST_ADMIN_USER and BITNEST_ADMIN_PASS on startup and seeds admin if users table empty. Build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Installer core logic functions + full test suite</name>
  <files>
    installers/linux-x86_64/install.py
    installers/linux-x86_64/tests/__init__.py
    installers/linux-x86_64/tests/conftest.py
    installers/linux-x86_64/tests/test_validation.py
    installers/linux-x86_64/tests/test_preflight.py
    installers/linux-x86_64/tests/test_secrets.py
    installers/linux-x86_64/tests/test_compose_template.py
    installers/linux-x86_64/tests/test_state.py
    installers/linux-x86_64/tests/test_distro.py
    installers/linux-x86_64/tests/test_health.py
    installers/linux-x86_64/tests/test_filesystem.py
  </files>
  <read_first>
    .planning/phases/10-linux-x86-64-installer/10-RESEARCH.md
    .planning/phases/10-linux-x86-64-installer/10-CONTEXT.md
    .planning/phases/10-linux-x86-64-installer/10-VALIDATION.md
    compose.yaml
  </read_first>
  <behavior>
    --- Validation ---
    - validate_port(5000) returns True; validate_port(0) returns False; validate_port(65536) returns False; validate_port(1023) returns False
    - validate_path("~/bitnest") returns expanded absolute path; validate_path("") returns error
    - validate_username("abc") returns True; validate_username("ab") returns False
    - validate_password("12345678") returns True; validate_password("1234567") returns False

    --- Preflight ---
    - is_port_free(port) returns False when port is bound by another socket
    - is_port_free(port) returns True when port is available
    - check_disk_space("/") returns (bool, float) tuple
    - check_docker() returns (docker_installed: bool, compose_v2: bool)

    --- Secrets ---
    - generate_secret() returns 64-char hex-only string matching [0-9a-f]{64}

    --- Compose Template ---
    - render_compose(api_port=5000, frontend_port=3000, docker_hub_user="denedi22") produces valid YAML
    - rendered compose contains "pg_isready -U bitnest"
    - rendered compose contains "condition: service_healthy"
    - rendered compose contains "${POSTGRES_PASSWORD}" (Docker variable intact)
    - rendered compose contains "denedi22/bitnest_api:latest"
    - rendered compose does NOT contain "~/" or version: key
    - rendered compose contains "restart: unless-stopped" for all 3 services
    - rendered compose contains BITNEST_ADMIN_USER and BITNEST_ADMIN_PASS on api service

    --- State File ---
    - write_state + read_state round-trip preserves install_dir, api_port, frontend_port, compose_file
    - read_state returns None when file does not exist
    - state_path() returns Path ending in .config/bitnest/install.json

    --- Distro Detection ---
    - ID=ubuntu → "apt_ubuntu"; ID=debian → "apt_debian"; ID=fedora → "dnf_fedora"
    - ID=arch → "pacman"; ID=rhel → "dnf_rhel"; ID=unknown → "fallback"
    - ID_LIKE containing "debian" → apt path

    --- Health Poll ---
    - parse docker compose ps JSON: service with Health="healthy" → True
    - service with State="running" and Health="" → True (no healthcheck)
    - service with State="exited" → False

    --- Filesystem ---
    - create_install_dirs creates data/storage and data/postgres subdirs
    - write_env_file produces file with chmod 600
    - write_env_file contains DATA_DIR, POSTGRES_PASSWORD, AUTH_SIGNING_KEY, BITNEST_ADMIN_USER, BITNEST_ADMIN_PASS

    --- Docker Install Commands ---
    - get_docker_install_commands("apt_ubuntu") returns list starting with ["sudo", "apt-get", ...]
    - get_docker_install_commands("pacman") returns list containing ["sudo", "pacman", ...]
    - get_docker_install_commands("fallback") returns curl get.docker.com command
  </behavior>
  <action>
    Create `installers/linux-x86_64/install.py` with all core logic functions. The file will later have the Textual TUI added in Plan 02, so structure the functions ABOVE a `# --- TUI --- #` marker comment. Include these functions:

    **Validation functions:**
    - `validate_port(value: str) -> tuple[bool, str]` — returns (True, "") or (False, "Enter a port number between 1024 and 65535") per UI-SPEC copywriting contract
    - `validate_path(value: str) -> tuple[bool, str]` — expanduser+resolve, returns (True, str(resolved)) or (False, "Enter a valid path (e.g. ~/bitnest)")
    - `validate_username(value: str) -> tuple[bool, str]` — min 3 chars, returns (True, "") or (False, "Username must be at least 3 characters")
    - `validate_password(value: str) -> tuple[bool, str]` — min 8 chars, returns (True, "") or (False, "Password must be at least 8 characters")

    **Preflight functions:**
    - `is_port_free(port: int) -> bool` — socket.bind() per Pattern 5 from RESEARCH.md
    - `check_disk_space(path: str = "/", min_gb: float = 5.0) -> tuple[bool, float]` — shutil.disk_usage
    - `check_docker() -> tuple[bool, bool]` — (docker_installed, compose_v2_available) per RESEARCH.md
    - `check_compose_version() -> str` — capture `docker compose version` output

    **Secret generation:**
    - `generate_secret() -> str` — `secrets.token_hex(32)` per D-23

    **Compose template:**
    - `COMPOSE_TEMPLATE` — triple-quoted string constant per Pattern 3 from RESEARCH.md. Exact template from RESEARCH.md lines 260-308. Docker Hub user `denedi22` per D-13. Uses `{api_port}`, `{frontend_port}`, `{docker_hub_user}` as Python format vars. Docker vars as `${{VAR}}`. Includes pg_isready healthcheck (D-17), restart: unless-stopped (D-18), BITNEST_ADMIN_USER/PASS on api (D-19), env_file: .env, bind mount volumes using ${{DATA_DIR}}.
    - `render_compose(api_port: int, frontend_port: int, docker_hub_user: str = "denedi22") -> str` — returns COMPOSE_TEMPLATE.format(...)

    **.env writer:**
    - `write_env_file(env_path: Path, cfg: dict) -> None` — writes DATA_DIR, POSTGRES_PASSWORD, AUTH_SIGNING_KEY, BITNEST_ADMIN_USER, BITNEST_ADMIN_PASS. Calls `os.chmod(str(env_path), 0o600)` immediately after write. Per Pattern 4 from RESEARCH.md.

    **Filesystem:**
    - `create_install_dirs(install_dir: Path) -> None` — creates install_dir/data/storage and install_dir/data/postgres with parents=True, exist_ok=True per INST-05

    **State file:**
    - `state_path() -> Path` — `Path(os.environ.get("XDG_CONFIG_HOME") or (Path.home() / ".config")) / "bitnest" / "install.json"` per D-20
    - `write_state(install_dir: str, api_port: int, frontend_port: int, compose_file: str) -> None` — per Pattern 7
    - `read_state() -> dict | None` — per Pattern 7

    **Distro detection:**
    - `read_os_release() -> dict` — parse /etc/os-release per Pattern 6
    - `get_docker_install_path(info: dict) -> str` — returns "apt_ubuntu", "apt_debian", "dnf_fedora", "dnf_rhel", "pacman", or "fallback" per Pattern 6

    **Docker install commands:**
    - `get_docker_install_commands(path: str) -> list[list[str]]` — returns list of command lists for the given install path. Commands per distro:
      - apt_ubuntu/apt_debian: `["sudo", "apt-get", "update"]`, `["sudo", "apt-get", "install", "-y", "ca-certificates", "curl"]`, `["sudo", "install", "-m", "0755", "-d", "/etc/apt/keyrings"]`, then GPG key download, apt source add, `["sudo", "apt-get", "update"]`, `["sudo", "apt-get", "install", "-y", "docker-ce", "docker-ce-cli", "containerd.io", "docker-compose-plugin"]`
      - dnf_fedora: `["sudo", "dnf", "-y", "install", "dnf-plugins-core"]`, `["sudo", "dnf-3", "config-manager", "--add-repo", "https://download.docker.com/linux/fedora/docker-ce.repo"]`, `["sudo", "dnf", "install", "-y", "docker-ce", "docker-ce-cli", "containerd.io", "docker-compose-plugin"]`, `["sudo", "systemctl", "start", "docker"]`, `["sudo", "systemctl", "enable", "docker"]`
      - dnf_rhel: same as fedora but with centos repo URL
      - pacman: `["sudo", "pacman", "-Sy", "--noconfirm", "docker", "docker-compose"]`, `["sudo", "systemctl", "start", "docker"]`, `["sudo", "systemctl", "enable", "docker"]`
      - fallback: `["curl", "-fsSL", "https://get.docker.com", "-o", "/tmp/get-docker.sh"]`, `["sudo", "sh", "/tmp/get-docker.sh"]`

    **Health poll:**
    - `parse_compose_ps_json(stdout: str) -> dict[str, bool]` — parse JSON lines from `docker compose ps --format json`, per RESEARCH.md health poll. Service healthy if Health=="healthy" OR (State=="running" AND Health=="").
    - `poll_health(compose_file: str, timeout: int = 60, use_sudo: bool = False) -> dict[str, bool]` — loop calling docker compose ps, returns per-service status dict

    **Subprocess helper:**
    - `run_cmd(cmd: list[str], check: bool = True, capture: bool = False) -> subprocess.CompletedProcess` — wrapper per D-24 (never shell=True, never cwd=)

    Then create the full test suite. Each test file tests the corresponding functions above using pytest. Use `tmp_path` fixture for filesystem tests. Use `monkeypatch` for mocking subprocess, socket, and file reads. conftest.py provides shared fixtures:
    - `mock_os_release(monkeypatch)` — patches Path("/etc/os-release").read_text
    - `mock_subprocess(monkeypatch)` — patches subprocess.run to return configurable CompletedProcess

    Tests must be written FIRST (RED), then functions implemented to make them GREEN. Follow TDD cycle.
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/CsharpProjekte/BitNest && python -m pytest installers/linux-x86_64/tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - installers/linux-x86_64/install.py exists and contains `def validate_port(`
    - installers/linux-x86_64/install.py contains `def is_port_free(`
    - installers/linux-x86_64/install.py contains `def generate_secret(`
    - installers/linux-x86_64/install.py contains `COMPOSE_TEMPLATE`
    - installers/linux-x86_64/install.py contains `pg_isready -U bitnest`
    - installers/linux-x86_64/install.py contains `condition: service_healthy`
    - installers/linux-x86_64/install.py contains `def write_env_file(`
    - installers/linux-x86_64/install.py contains `def create_install_dirs(`
    - installers/linux-x86_64/install.py contains `def state_path(`
    - installers/linux-x86_64/install.py contains `def read_os_release(`
    - installers/linux-x86_64/install.py contains `def get_docker_install_path(`
    - installers/linux-x86_64/install.py contains `def get_docker_install_commands(`
    - installers/linux-x86_64/install.py contains `def parse_compose_ps_json(`
    - installers/linux-x86_64/install.py contains `def poll_health(`
    - installers/linux-x86_64/install.py contains `secrets.token_hex(32)`
    - installers/linux-x86_64/install.py contains `0o600`
    - installers/linux-x86_64/install.py contains `BITNEST_ADMIN_USER`
    - installers/linux-x86_64/install.py contains `BITNEST_ADMIN_PASS`
    - installers/linux-x86_64/install.py contains `restart: unless-stopped`
    - installers/linux-x86_64/install.py contains `denedi22/bitnest_api:latest`
    - installers/linux-x86_64/tests/test_validation.py exists
    - installers/linux-x86_64/tests/test_compose_template.py exists
    - installers/linux-x86_64/tests/test_distro.py exists
    - installers/linux-x86_64/tests/test_state.py exists
    - installers/linux-x86_64/tests/test_health.py exists
    - installers/linux-x86_64/tests/test_filesystem.py exists
    - `python -m pytest installers/linux-x86_64/tests/ -x -q` exits with 0 (all tests pass)
  </acceptance_criteria>
  <done>All core installer logic functions implemented in install.py. All unit tests pass. Functions cover validation, preflight, secrets, compose template, state file, distro detection, Docker install commands, health polling, filesystem ops.</done>
</task>

</tasks>

<verification>
- `dotnet build BitNest/BitNest.csproj --no-restore` succeeds
- `python -m pytest installers/linux-x86_64/tests/ -x -q` — all tests green
- `grep -c "BITNEST_ADMIN_USER" BitNest/Program.cs` returns >= 1
- `grep -c "COMPOSE_TEMPLATE" installers/linux-x86_64/install.py` returns >= 1
- `grep -c "pg_isready" installers/linux-x86_64/install.py` returns >= 1
</verification>

<success_criteria>
- API builds and seeds admin from env vars on startup when users table is empty
- All 8+ test files pass covering validation, preflight, secrets, compose template, state file, distro detection, health polling, filesystem
- install.py contains all core logic functions ready for TUI wiring in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/10-linux-x86-64-installer/10-01-SUMMARY.md`
</output>
